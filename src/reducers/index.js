// Commented out because no actions written yet
// import * as actions from '../actions';

const initialState = {};

export const hatchmydragonReducer = (state=initialState, action) => {
  return state;
}